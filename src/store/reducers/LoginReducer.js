const initialState ={
}



const LoginReducer = (state = initialState, action) => {

    console.log(state)
    switch (action.type) {

        default:
            
            return state
    }
}

export default LoginReducer;